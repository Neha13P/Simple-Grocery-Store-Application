{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch in Edge",
            "type": "msedge",
            "request": "launch",
            "url": "http://localhost:5000",
            "webRoot": "${workspaceFolder}"
        }
    ]
}
